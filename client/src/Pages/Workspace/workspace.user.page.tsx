import { WorkspaceResponse } from "../../Types/Workspace";

export default function WorkspaceUserPage({ workspace }: { workspace: WorkspaceResponse }) {
      
      return (
        <div style={{ padding: "20px" }}>
          <h1 style={{ fontWeight: "bold", color: "white" }}>Hello</h1>
        </div>
      );
      
}